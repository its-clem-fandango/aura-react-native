import { View, Text } from "react-native";
import React from "react";

//putting the filename in brackets [] also borrows from nextjs. It creates a dynamic route
const Search = () => {
  return (
    <View>
      <Text>Search</Text>
    </View>
  );
};

export default Search;
